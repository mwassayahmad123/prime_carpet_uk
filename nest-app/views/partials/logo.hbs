<img
  src="{{logo.src}}"
  alt="{{logo.alt}}"
  class="logo-image js-logo"
  onerror="this.replaceWith(Object.assign(document.createElement('span'), { className: 'logo-text', innerHTML: 'Prime<span>Carpet Cleaning</span>' }))"
/>
